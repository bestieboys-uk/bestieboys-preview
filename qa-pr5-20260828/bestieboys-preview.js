const header=document.querySelector('[data-header]');const reveals=[...document.querySelectorAll('.reveal')];const glow=document.querySelector('.glow');const observer=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(!entry.isIntersecting)return;entry.target.classList.add('visible');observer.unobserve(entry.target)})},{threshold:.12});reveals.forEach(el=>observer.observe(el));window.addEventListener('scroll',()=>header?.classList.toggle('scrolled',window.scrollY>24),{passive:true});if(glow&&window.matchMedia('(pointer:fine)').matches){window.addEventListener('pointermove',e=>{glow.style.left=`${e.clientX}px`;glow.style.top=`${e.clientY}px`},{passive:true})}const flagship=document.querySelector('.flagship-product');if(flagship){flagship.tabIndex=0;flagship.setAttribute('role','link');flagship.setAttribute('aria-label','Open Tour Issue product prototype');flagship.style.cursor='pointer';const open=()=>location.href='tour-issue.html';flagship.addEventListener('click',open);flagship.addEventListener('keydown',e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();open()}})}